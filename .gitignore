# virtualenv
.venv/
venv/

# python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# terry local data (candles, sessions, reports, charts, config)
storage/*.db
storage/*.db-wal
storage/*.db-shm
storage/reports/*
storage/backtest-charts/
storage/config.json
!storage/.gitkeep
!storage/reports/.gitkeep

# user strategies are yours to keep; comment out the next line to version them
# strategies/

# browser-automation scratch (playwright-cli sessions/snapshots)
.playwright-cli/

# os
.DS_Store
.env
